package com.rest.order.dao;

//import com.auth0.jwt.JWT;
//import com.auth0.jwt.interfaces.DecodedJWT;

public class JWTUtil {

    public static String getDecodedJWTUserName(String authToken) {
//        DecodedJWT decodedToken = JWT.decode(authToken.substring("Bearer ".length()));
//        return decodedToken.getClaims().get("username").asString();
        return null;
    }
}
